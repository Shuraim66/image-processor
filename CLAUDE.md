# Project — The Toy Gift Shop (image-processor)

Shopify store: `the-toy-gift-shop-cy0w57r4.myshopify.com` (PKR). Working dir: `/Users/junaidali/Desktop/image-processor`. Branch: `feature/raw-footages-ingest`.

## Current state
- **94 active products** in `input/`, all DRAFT, priced + stocked, all with complete generated images (82 on 2026-09-22, +13 from the new-product intake below, -1 to `backlog-products-image-failed/`).
- Stock total: 431 pcs. Retail value at listed prices: Rs 1,034,000.
- CSVs rebuilt (`catalog_products.csv`, `catalog_variants.csv`, `catalog_needs_input.csv`) and kept in sync with the active set.
- ~~Housekeeping: `SQUISHY-CRISPCHOCO-CLEARCASE` stray folder~~ — deleted 2026-09-23 per the user (was a pre-session leftover, one raw photo, no `product.json`, never a real product). Folder removed and its junk rows cleared from `catalog_products.csv`/`catalog_variants.csv`; `shopify_import.csv` already excluded it so no re-export needed.
- `backlog-products/` holds sub-3-unit items (119 folders) — low stock, unrelated to image generation.
- **`backlog-products-image-failed/` (new, 2026-09-23)**: 5 folders moved out of `input/` and out of the catalog CSVs — these are fully priced/stocked/box-checked products whose `CatalogHero` generation hits fal/OpenAI's `content_policy_violation` every time (licensed/trademark-adjacent character designs), not a low-stock issue. Removed from the active catalog per the user's request so they don't get retried or exported until a manual image (reshoot, GPT, or a licensed web image) is ready: `AVENGERS-AGE-OF-ULTRON-MAGNETIC-DRAWING-BOARD` (`TGS-AVENGERS-MAG-DRAWING`), `CARS-DINOCO-RC-RADIO-CONTROL-CAR` (`TGS-CARS-RC-RADIO`), `DESPICABLE-ME-2-MINION-SLIDE-TOY` (`TGS-ME-SLIDE`), `WINNIE-THE-POOH-ACTION-FIGURE` (`TGS-WINNIE-POOH`), `TWO-IN-ONE-DEFORM-ROBOT-CAR` (`TGS-DEFORM-ROBOT-CAR`, title literally says "with Lightning McQueen Design"). Each folder keeps its `product.json` (verified copy/price/stock/tags) and any already-generated free/local images intact — nothing lost, just parked. To bring one back: move its folder back into `input/`, re-add its row(s) to `catalog_products.csv`/`catalog_variants.csv` (removed 2026-09-23), then run the pipeline once a compliant image exists.
- Two approved test images sent to fal.ai this session (the CatalogHero v2 tests on the 3D Pen, $0.0515 each — see Completed this session).
- **Draft Shelf review artifact updated 2026-09-23** (https://claude.ai/artifact/PgQUmjnjur5iHnUfjyo1FS) to match the current 94-product active catalog: removed the 5 stale entries now in `backlog-products-image-failed/`, added the 12 new-intake products with real thumbnail-strip images (50 new images uploaded), refreshed the header stats (94 products, 431 pcs, Rs 1,034,000). Existing approve/needs-fix/pending decisions for the other 82 products carried forward untouched (per-SKU, stored in the artifact's own db). Justice League (also content-policy-failed) was never added since it never made it into the active catalog.

## Rules (must stay in effect)
- `SESSION_RECOVERY.md` is historical context; do not edit it.
- `box-is-source-of-truth.md`: copy facts (contents, counts, features, age, name) come from the printed box, not partial photos.
- `fal-spend-approval.md`: never call fal.ai without the user's explicit approval of an exact figure. Paid slots only: CatalogHero, Lifestyle, FeatureProduct. Real cost: $0.0515/image (`FAL_MODELS` in `fal_listing.py`). `MAX_REFS=3`. Spend log in `_fal_usage.json` / `processed_products.csv`.
- `catalog-image-approach.md`: real photo + local template text — **CatalogHero is photography only.** `CATALOG_HERO_V2` in `fal_listing.py` (replacing the old CatalogHero formula on 2026-09-22 — see Completed this session) allows a richer styled photo (environment, props, composition, close-up detail insets) and may show real packaging in frame, but must **never add any AI-drawn text** — no headline, no feature callouts, no slogans, nothing beyond what's physically printed on the real product/packaging as photographed. Headline and feature-callout text stays with the free local `FeatureCard`/`Hero_titled` templates, which pull verified facts from `product.json` — deliberately kept separate so AI-generated text can never misspell something or invent a claim the box doesn't support. CatalogHero also must never move a packaging logo into a header/corner/watermark position, and must never treat packaging illustration art as photographic evidence of what's physically included (real product photos outrank packaging photos outrank packaging artwork). Other slots (Lifestyle, FeatureProduct, Angle, Open, Box, Detail) are unchanged.
- `stock-and-backlog.md`: list only `units > 2`; `stock = units - 1`; rest to `backlog-products/`.
- `shopify-store-conventions.md`: 31 fixed Types; prefixed tags (`age:`, `occasion:`, `feature:`, `play:`, `sub:`); handle = folder lowercased, no ages; SKU is short code (e.g. `TGS-AQUAFLOW-SWIM-CAP`).
- Photo placement: revised images are extra angles, never replacements (`_archive/superseded_photos`).

## Completed this session (from SESSION_RECOVERY.md)
- Drone corrected to single red product (`TGS-FOUR-AXIS-DRONE`), no variants; `box_back` labelled on angle2.
- Football figures: `TGS-FIG-FOOTBALL-STAR` is one listed product ("STARS Football Figure... Ronaldo or Mbappé") with 3 variants per the current CSVs — Ronaldo/Al-Nassr (stock 2), Ronaldo/Man United (stock 2), Mbappé/Real Madrid (stock 2); Portugal figure alone went to backlog (1 pc). This corrects an earlier note in this file that had Man United going to backlog — the CSV (source of truth) lists it.
- Box-check completed for all 68-at-the-time products; text corrected against printed boxes (e.g. Avengers 380 ball pops; rattle set 3 turntable rattles; insect game 1 wooden board; camera green with pink lanyard; Minion Slide has light/sound; Labubu electric bubble gun; shape sorter ViVi name).
- Folder names cleaned to HYPHENATED-CAPS with brand first; SKU/handle split enforced.
- New squishies created from archive: Mochi Squishy, Crackle Fluff Crispy Crunch, Capsule Squishy Ball.
- WhatsApp price matching done; `happy_toys_prices.csv` updated and gitignored.
- Catalog CSVs rebuilt with SEO title form (`<Name> in Pakistan | Toy Gift Shop`) and meta descriptions.
- 21 more products added and box-checked, bringing the catalog to 87 (see Current state).
- Found and fixed one listing-text glitch while reviewing: `TGS-EDU-KEYS` description said "An Educação learning tablet" (a stray Portuguese word) — corrected to "An educational learning tablet" in `catalog_products.csv`.
- **Review page artifact rebuilt for all 87 products** — photos (first "front" shot per product, resized), prices, stock, tags, full description/SEO text, and variants where they exist. Published as a claude.ai Artifact ("Draft Shelf") with per-SKU Approve/Needs-fix/Pending decisions and a notes field, persisted via the artifact's `db` capability so decisions survive reloads: https://claude.ai/artifact/PgQUmjnjur5iHnUfjyo1FS (private — share it from the page's Share menu if anyone besides the owner needs to open it). Since extended with a thumbnail strip per product (all raw shoot photos + brand photos, 397 images total) and a fix for a cosmetic bug that showed "1 variants" on every single-SKU product.
- Wired 3 real supplied photos into the pipeline at zero fal cost (found sitting unused in `brand/` subfolders from last session): `TGS-AQUAFLOW-SWIM-CAP` (Intex worn shot → Lifestyle), `TGS-TOY-STORY-FRICTION` (MKB two-angle shot → CatalogHero), `TGS-SQUISHY-OREO` (Schylling black → CatalogHero, purple/pink → extra gallery images). Verified with `fal_listing.py --plan` and `slots_to_generate()` that no paid slot triggers for any of the three.
- **Replaced the CatalogHero prompt (`CATALOG_HERO_V2` in `fal_listing.py`)** after the user reviewed a 3D Pen test generation where the model promoted the box's real "tuosiqi" logo into a top-left store-logo position. New prompt: explicit product/packaging/branding distinction, a reference-priority order (real product photos > accessories shown > packaging photos > packaging artwork) so a box illustration can't override an actual photo, and an explicit ban on relocating any packaging logo into a header/corner/watermark/badge position.
  - Ran one approved test ($0.0515) on the 3D Pen with v2: correctly kept the logo on the box, didn't invent a second pen or extra filament colors, didn't turn the astronaut box art into a physical figurine — but used a styled marble-and-plant setting, inconsistent with the plain white "pattern" already approved for other products (the Dancing Cactus `hero_test/B.jpg`).
  - User then compared it against `hero_test/A.jpg` (the earlier ChatGPT-prompt cactus test, with a drawn headline, feature-callout circles and invented slogans) and initially wanted that same headline/callout richness baked into CatalogHero. Tried adding a directive "build a mini-infographic" section plus a `{facts}` placeholder wired to `product.json` (title/age/whats_included/bullet_points) so any AI text would at least be grounded — but on reflection the user chose to keep CatalogHero **photography-only** instead, precisely to avoid the AI ever misspelling something or inventing a claim the box doesn't support. Reverted the facts-injection plumbing (removed `_verified_facts()`, `build()` no longer passes `facts=`) and rewrote section 5 to explicitly forbid any added text/typography — richer environment and props stay allowed, headline/callouts stay with the free local `FeatureCard`/`Hero_titled` templates.
  - Ran a second approved test ($0.0515) on the 3D Pen with the revised prompt (`hero_test/3d-pen-v2b.jpg`): plain white studio background again, matching the cactus's approved "pattern" — no marble, no plant — while still correctly keeping the tuosiqi logo on the box, one pen only, correct filament colors, no invented text anywhere. This is the version to carry into the full batch.
  - Net effect: CatalogHero v2 is now "real product + real packaging, professionally re-photographed on a plain seamless background, zero AI-written words." Other slot prompts (Lifestyle, FeatureProduct, Angle, Open, Box, Detail) untouched throughout.
  - Wired `hero_test/3d-pen-v2b.jpg` in as `input/3D-PRINTING-PEN/output/CatalogHero.png` and ran the real `fal_listing.build()` for this one product to see the full intended package together — confirmed **0 fal calls** (the pre-placed CatalogHero satisfied the paid-slot skip-guard). Produced, all free: `CatalogClean.png` (the real Shopify main image — clean cutout, inherits the hero's plain background), `Open.png`/`Box.png` (local cutouts from the original photos), `FeatureCard.png` and `Hero_titled.png` (headline + feature callouts + store logo, all pulled from `product.json`'s real bullet points/age/whats_included — this is where the "A.jpg-style" headline/callout look now lives, safely). This SKU's `output/` is a first full example of the finished v2 package; the other 86 still need the same treatment once a batch figure is approved.
- **Wired `brand.py` into the pipeline** (it existed since last session — tile+chip+corner-logo functions — but was never imported anywhere; `fal_listing.build()`'s watermark step only did a plain corner-logo composite before this). Now `build()` calls `brand.tile_wordmark()` + `brand.chip()` + `brand.corner_logo()` on every `_wm` marketing variant. Scope, per the user: marketing variants only (`CatalogHero_wm`, `Angle_wm`, `Open_wm`, `Box_wm`, `Detail_wm`, `Lifestyle_wm`) — never `CatalogClean` (must stay unwatermarked for Google/Meta Shopping feeds), never `FeatureCard`/`Hero_titled` (out of scope for this change). Shrank the chip pill on request: `CHIP_HEIGHT` 0.075 → 0.055, and tied its font size to the chip's own height (`h * 0.28`) instead of image width, so the whole pill — logo, text, padding — scales together instead of the text staying a fixed size while the pill shrinks.
- **Fixed a real bug in `shopify_export.py`** found while explaining the branding change: `images_for()` used to list every file in a product's `output/` folder alphabetically with no idea which was "the main image" or which was branded — so once `_wm` files existed, both the plain and branded copy of the same photo would export as two separate gallery images, and the main image ended up being whichever filename sorted first alphabetically (`Box.png`, a packaging shot, not `CatalogClean.png`). Rewrote it to always put `CatalogClean` first, then walk each marketing slot preferring its `_wm` copy over the plain one (never both), then `Hero_titled`/`FeatureCard`, with anything unrecognized (e.g. the Oreo's `Variant-Purple.png`/`Variant-Pink.png`) appended after. Verified end-to-end with `shopify_export.py --sku 3D-PRINTING-PEN`: 6 rows, correct order, no duplicates.
- **Batch scoping decision**: rather than running all 87 at once, split into "proceed now" (has, or can get, a real usable hero) and "defer" (box-only, no unpackaged photo — a hero made now would just be replaced once reshot). "Brown Tumbler" and the die-cast cars the user mentioned as prior test products turned out to be in `backlog-products/`/`_archive/`, not the current 87 — confirmed this doesn't change the 87-product scope.
  - Found and fixed a real `photo_tags.py` mistagging bug while building the defer list: `TGS-CUTE-INTELLIGE-ROBOT`'s `angle3.jpeg` had `photo_packaging: false` (correct — the user caught that the photo clearly shows the robot, remote, ball and USB cable fully out of the box) but was tagged view `product_with_box` instead of `product_front`, so `plan_slots()`'s exact-view-match filter silently skipped a perfectly good photo. Retagged to `product_front`; swept all 87 products for the same pattern (packaging=false + a non-product-only view tag) and found no other real cases — the other 5 hits were `contents`-tagged brand photos I'd deliberately made inert earlier this session, not bugs. This moved the robot from "defer" to "proceed now" — it now gets both CatalogHero and Lifestyle.
  - **Reshoot Queue artifact published** (https://claude.ai/artifact/QKn3x3HySNUJ9mzrBY8EgJ, `db`-backed) for an initial 17-product defer list — but the user then reviewed the actual photos against it directly and overturned most of it: 16 of 17 have the real product visibly identifiable in at least one photo (even with the box in frame), which `CATALOG_HERO_V2` section 2 explicitly handles ("remove a visible toy from a transparent tray... arrange visible accessories beside it") — confirmed by eye on `TGS-FOLLOW-ME-CAR` (open display tray, product loose) vs `TGS-ORANGE-MONSTER-TRUCK` (genuinely sealed in a hard blister, never removed in any photo — still usable per section 2, just a stricter case). **Net decision: nothing deferred, all 87 proceed.** Only `TGS-FIG-FOOTBALL-STAR` is a real asterisk — every photo is box art only, so its hero is honestly a photo of the box, not the figures. None of these 17 get a Lifestyle image (no photo shows the product genuinely out of package), which is a separate, smaller gap. The Reshoot Queue artifact is now moot but stays published.
  - Side investigation while reviewing `TGS-FOLLOW-ME-CAR`: the user pasted a "clean" reference image showing the car with a marker pen. Traced it to a manufacturer marketing collage (not the user's own photo) for a similarly-named but different product (a line-following "Magic Induction" car) — confirmed against the box-checked `whats_included` (car, remote, ball, USB cable, no pen) and the user's own box check: no marker in this unit. Correctly kept unused — never fed to fal, never treated as this product's reference. Good example of the copyright/fidelity risk already established this session working as intended.
  - **Approved: $7.26 for all 87 (141 paid images: 82 CatalogHero + 59 Lifestyle).** Batch started via `run_batch.py`.
- **Batch run incident, mid-run**: ~19 products in, a real cost leak surfaced — several images were billed successfully by fal but then failed to *download* (`v3b.fal.media`'s CDN round-robins several edge IPs; some reset the TLS handshake under load). The old retry (3 attempts, no delay) wasn't enough. Stopped the batch immediately.
  - **Recovered all 6 affected images** — each error includes the exact fal.media URL of the already-generated file; retried each with `curl` and all 6 saved fine (confirms the images were never actually lost, just the one-shot download). **$0 was actually wasted.**
  - **Fixed `_fal_scene`'s download retry** in `fal_listing.py`: 8 attempts with backoff (2s→15s cap) instead of 3 instant tries; the final error message now includes a ready-to-run `curl` command with the recovery URL, so a future failure is a one-line fix, not a mystery.
  - Re-ran `build()` for the 6 recovered products to finish the free local steps their crash interrupted (`CatalogClean`, `FeatureCard`, `Hero_titled`, branding) — 3 of them still had a genuinely-pending Lifestyle image (never attempted before the crash), correctly billed once ($0.1545 total, real and expected, not a duplicate).
  - **Separate, non-money issue**: `TGS-AVENGERS-MAG-DRAWING` and `TGS-CARS-RC-RADIO` hit `content_policy_violation` from fal (confirmed **$0.00 cost** via the user's fal dashboard screenshot) — both are licensed-character products (Avengers, Cars/Lightning McQueen livery); OpenAI's image moderation likely flags recognizable copyrighted characters generally, so other licensed toys (Toy Story, PAW Patrol, Winnie the Pooh, Kuromi, Minions) may hit the same wall later in the batch. Zero cost either way, but zero image either way too.
  - **Resumed** via a `RESUME_FROM=19` variant of `run_batch.py` (skips the first 19 already-done rows to avoid redundant free-step re-rendering; the existence-guard makes this safe either way) — instructed by the user to let it run straight through any further failures (content-policy or otherwise) without stopping, and compile a flagged list of everything that didn't generate for manual handling afterward, rather than fixing each one inline.
- **Batch run COMPLETE (2026-09-23).** 78/87 products fully generated (`CatalogClean`/`CatalogHero`/etc. all present); 9 flagged, all confirmed **$0 cost** — none were billed, so nothing was lost, only skipped. Final spend today: **$6.4375** (125 paid images actually billed, vs. the $7.26/141-image estimate — the gap is exactly the images that never got billed because their product failed before generation or had no Lifestyle photo). Lifetime fal total: **$9.7085** of $12 (was $3.2710 before today).
  - **Found and fixed a real code bug** while triaging two of the flagged products (`ValueError: can't measure length of multiline text` on `TGS-LIGHTSABE`/`TGS-IRON-APPLIANCE`): `product.json`'s `tagline_top` is intentionally two-line (`"LIGHTSABER\nTOY"`) for the older two-line banner in `make_hero.py`, but two newer single-line-pill renderers in `feature_card.py` (`render_v2`'s eyebrow at line ~233 and `render_hero_title`'s eyebrow at line ~316) passed that same string straight into the letter-tracking helper (`_tracked`/`_tracked_width`, which calls `draw.textlength()` per character) — and `textlength()` raises on any string containing `\n`, including a lone newline character. Fixed both call sites to flatten `\n` → space before use. Re-ran `fl.build()` for both products afterward: **0 new paid images** (their `CatalogHero`/`Lifestyle` were already billed and saved before the original crash — the existence guard skipped them, only the free local steps re-ran). Both are now fully complete and out of the flagged list.
  - **Retried all 9 flagged products (2026-09-23), approved $0.77 worst case, actual spend $0.309 (6 images):**
    - **4 succeeded** — the "Invalid storage type"/`downstream_service_error` group really was transient: `TGS-MAGIC-WAND-CROWN`, `TGS-MAG-BUILDING-BLOCKS`, `TGS-UNICORN-NIGHTLIGHT`, `TGS-STEERING-WHEEL-SIM`. All fully complete now (verified every expected output file present).
    - **5 still fail — all `content_policy_violation`, all confirmed $0 cost, need manual images**: `TGS-AVENGERS-MAG-DRAWING`, `TGS-CARS-RC-RADIO`, `TGS-ME-SLIDE`, `TGS-WINNIE-POOH` (licensed characters, as before), plus **`TGS-DEFORM-ROBOT-CAR`** — its earlier DNS error had been masking the real problem; once the connection went through, it hit the same content-policy wall. `product.json` has no brand name (`"TWO IN ONE DEFORM ROBOT CAR"`, generic `whats_included`), so this is almost certainly OpenAI's vision moderation reading the reference photos' car-to-robot transformation as Transformers-style trademarked IP, not a text-prompt trigger. Same bucket as the other 4 — needs a manually-sourced/edited image, not a pipeline fix.
  - Catalog now stands at **82/87 fully generated, 5 flagged** (all licensed/trademark-adjacent characters, all $0 cost, all need manual images rather than a retry).
- **Moved the 5 flagged products to a new `backlog-products-image-failed/` folder** (per the user's request) and removed their rows from `catalog_products.csv`/`catalog_variants.csv` — the active catalog is now exactly 82 products, all fully imaged, none pending a fal retry. See Current state for the full list and how to bring one back later.

## Pending tasks
1. ~~Rebuild review page artifact~~ — done 2026-09-22 for all 87 products; now stale by 5 (link above, due for a refresh to 82 next time it's touched).
2. ~~Catalog image generation~~ — **done 2026-09-23.** 82 active products, all fully generated. The 5 that hit `content_policy_violation` were moved to `backlog-products-image-failed/` (see Current state) rather than left flagged in the active set — they need a manually-sourced image, not a pipeline retry.
3. Check/review listing text for remaining inconsistencies — one fixed this pass (`TGS-EDU-KEYS`, see above); not an exhaustive pass, just what surfaced while building the review artifact.
4. Git blocked (`sudo xcodebuild -license`); nothing from this session is committed.
5. NeeDoh Oreo set to 24 pcs / stock 23 (done).
6. **New product intake — next up.** User says raw photos are going into `~/Desktop/new-products/` plus price/unit screenshots on the Desktop, to be posted once this image batch was done (it now is). Do not go looking for the screenshots — wait for the user to post them. Same workflow as earlier this session's 21-product intake: match photos to price-list screenshots, apply the stock rule, box-check names/text, fold into the CSVs.
7. User mentioned combining CSVs once the image batch is done ("once done we have to combine that CSVs as well") — likely means re-running `shopify_export.py` across the 82 active products for one final combined Shopify-import CSV; not yet confirmed or run, ask before assuming scope.
8. **The 5 in `backlog-products-image-failed/`** wait on the user to reshoot, generate via ChatGPT, or source a compliant web image — once ready, bring the folder back into `input/`, re-add its CSV row(s), and run `fal_listing.py` normally.

## Spending
- fal.ai lifetime total: **$10.996 of $12** ($3.2710 before today + $6.4375 main batch + $0.309 retry of the 9 flagged + $0.9785 new-product intake batch).
- Today: approved $7.26 for the main batch (billed $6.4375) + approved $0.77 for the 9-product retry (billed $0.309) + approved $1.08 for the 13-product new-product intake (billed $0.9785, 19 of 21 possible images — only Justice League failed content-policy at $0).
- Budget remaining: **≈$1.00**. Getting tight — flag to the user before approving further batches.
- Historical tests' conclusion (from `SESSION_RECOVERY.md`, since there's no separate file despite the "Rules" line below naming one): a ChatGPT-proposed hero prompt redrew packaging and invented slogans, so it lost to our prompt + its one good "packaging beside/behind" line; cheaper models (`qwen-image-edit-2511`, `muse-image`) failed the fidelity test, so `gpt-image-2.5-sunburst-medium` ($0.0515/image) is still the only model in use.

- **Rebuilt `shopify_export.py` from scratch (2026-09-23)** — the old version was obsolete: it read only `product.json` + a single global `--price`/`--qty` CLI flag, never knew about the real per-SKU PKR prices/stock/tags built up in `catalog_products.csv`/`catalog_variants.csv` this session, and had no variant support at all (every product exported as one flat "Default Title" row). New version reads `catalog_products.csv` + `catalog_variants.csv` as the source of truth for price/stock/tags/SEO/status, keeps the working `images_for()` gallery-ordering logic, and pulls `alt_text`/`bullet_points`/`whats_included` from each product's `product.json` to build a richer HTML body.
  - **Found and fixed a bug while building it**: `catalog_variants.csv` actually has one row per product for *all* 82 (even single-SKU ones, `variant="Default Title"`) — not just the 4 real multi-style products. First pass treated "has any variants.csv row" as "is multi-variant," so every single product wrongly got `Option1 Name: Style` instead of `Title`. Fixed to key off `len(variants) > 1` instead. Verified: 78 products correctly `Title`/`Default Title`, 9 variant rows across the 4 real multi-style products (chocolate-bar-squishy, crisp-choco-squishy, household-set-mini-appliance, football-figure) correctly `Style`.
  - Ran it for all 82 active products: **516 rows written to `shopify_import.csv`** (replacing the Aug-26 stale file). Verified: all 511 image references resolve to real files on disk (0 missing), every product has ≥2 images, no blank-handle or missing-price rows.
  - **Image Src is a local path** (`input/<FOLDER>/output/<file>.png`), by the user's own choice — devs will host the images themselves and swap in the real base URL (re-run with `--image-base-url https://...` once that host exists). Not importable to Shopify as-is until that swap happens.
  - Note: this was run against the 82-product catalog, before the new-product intake below added 13 more. Re-run before handoff if the devs want the full current set.

## New-product intake — 13 products added, $0.98 spent (2026-09-23)
- User supplied 58 raw photos (`~/Desktop/new-products/`) + 15 price/unit WhatsApp screenshots (`~/Desktop/screenshots-price-units/`) covering **16 physical items**. Grouped all 58 photos into products by visual identity (NOT by timestamp gaps — verified this the hard way: one 9-photo timestamp cluster actually contained 2 different RC cars only 51s apart, about the same gap as normal within-product pauses, so every group was visually confirmed photo-by-photo).
- **14 real products, priced/stocked**: 2 went to backlog (≤2 pcs, per the stock rule) — Shooting Elite graffiti gel blaster (2pcs/Rs5400) and XINLEJIA Fishing Game (2pcs/Rs4950), both in `backlog-products/`. **12 listed** (2 of them cover 2 color variants each under one SKU/price since the supplier's own price photo bundled them: X Warrior Hero Elite Flame [white+blue], Shooting Elite MP7 [green camo+tan camo] — no per-color stock breakdown was given, so no variant split was invented).
- **A 16th item ("M-206 Electric Toy Gun," 4pcs/Rs5300) had a price screenshot but no photos** — the referenced filenames didn't exist anywhere on disk. User located and added them (`213654`/`213734`/`213759`) after being asked; processed normally once supplied.
- **Real corrections made during intake**:
  - A "4DRC VITA-50" drone in the new batch first looked like a restock of the existing `backlog-products/4DRC-VITA-50-4-AXIS-REMOTE-CONTROL-DRONE` (1pc/Rs5850, same brand+model name) — merged photos into it, then **the user corrected this: they are two different products/models with different prices and stock**. Reverted the merge, gave the new one its own folder/SKU (`TGS-VITA50-JET-DRONE`, jet-plane-shaped body vs the old one's plain quad body) and left the original backlog item untouched.
  - Flagged a possible duplicate, now resolved: new `TGS-SPIDERWEB-LAUNCH` (Cosplay Toys, hand-crank multi-dart wrist launcher) vs existing `TGS-SPIDER-LAUNCHER` (USB-charged retracting wire-and-suction mechanism) — **user confirmed 2026-09-23 these are a different model and a different toy.** Kept as separate SKUs, no merge, closed.
- **Used `analyzer.py`/`photo_tags.py` (local Ollama `qwen3-vl:8b-instruct-q4_K_M`, zero cost)** to draft product.json + view/packaging tags for all 13, then corrected real hallucinations against the actual box-check: "2.4GHz" invented for the Frenzy Racing car (box says 27MHz), a fabricated "costume and blue-tinted glasses" for the spider launcher (real items: web-print sleeve + hand-crank launcher + 3 suction darts), "MRC" typo for "4DRC" on the new drone, and generic/wrong `whats_included` lists (missing tape measure on the drill set, unnamed CoComelon characters — added TomTom/JJ/YoYo/Bingo by name from the box's own labelling). Also caught and fixed 2 real `photo_tags.py` mistags (Frenzy Racing and the M-206 blaster each had a genuinely packaging-free photo wrongly tagged `product_with_box`, which would have silently blocked their Lifestyle image).
- **Computed the real fal image-gen figure via `fal_listing.py --plan`** rather than accepting the user's own $1.28 guess: 21 paid images = **$1.08 ceiling**. Approved and run: **12/13 succeeded, $0.9785 actual spend.**
  - Only `TGS-JUSTICE-LEAGUE-FIGS` failed (`content_policy_violation`, $0 cost, DC characters) — moved to `backlog-products-image-failed/` and removed from the catalog CSVs, same treatment as the 5 licensed-character products from the main batch.
  - `TGS-COCOMELON-FIGS` (also a licensed-character set) **succeeded** — a reminder that content-policy hits aren't fully predictable by brand alone, worth still attempting rather than pre-emptively backlogging every licensed character.
  - All 12 successful products verified complete (`CatalogClean.png` present) before updating the catalog.

## Dev handoff: output/<SKU>/ (new, 2026-09-23)
- **`build_output_folders.py`** (new pipeline script): copies each active product's curated final image set from its working `input/<FOLDER>/output/` dir into a clean `output/<SKU>/` folder, keyed by real SKU rather than the handle-style folder name — so devs can map straight from folder name to Shopify Variant SKU with no lookup table. Reuses the exact same curation rule as the CSV (`shopify_export.curated_filenames()`, factored out for this): `CatalogClean` first, then each marketing slot's branded `_wm` copy (or plain if never branded), then `Hero_titled`/`FeatureCard`, then anything else alphabetically.
- Run: `python build_output_folders.py`. Idempotent (`shutil.copy2`, safe to re-run after any pipeline change) — re-run it whenever `input/*/output/` changes before re-exporting the CSV.
- **`shopify_export.py` now reads images from `output/<SKU>/`, not `input/<FOLDER>/output/`** — run `build_output_folders.py` first or the CSV's Image Src column will be empty.
- **Also fixed while wiring this up**: `shopify_export.py`'s `main()` used to export every row in `catalog_products.csv` unconditionally, including the stray `SQUISHY-CRISPCHOCO-CLEARCASE` junk entry (blank price/stock) as a garbage row. Now skips + reports any row missing price or stock before exporting.
- Ran both for the full active catalog: **580 images copied across 94 products, 585-row `shopify_import.csv` regenerated** — 0 missing images, 0 rows without a price, exactly 94 unique products (the junk row correctly excluded).
- **`split_import_csv.py` (new, 2026-09-23)**: writes a per-product `shopify_import.csv` into each `output/<SKU>/` folder too (reuses `shopify_export.rows_for()`, so it's a split of the same data, never a second source of truth) — so a single product's folder is self-contained (images + its own import rows) for devs who want to review or hand off one SKU at a time, without cross-referencing the combined root-level CSV. Verified on both a single-SKU product (6 rows: 1 product row + 5 image rows) and a multi-variant one (7 rows: 3 variant rows + 4 image rows, football figures). **Full build order: `build_output_folders.py` → `split_import_csv.py` → `shopify_export.py`** (the first two must run before re-zipping for a handoff, or the per-product CSVs go stale).

## Next intended step
**Handed to the user 2026-09-23** for forwarding to devs: `shopify_import.csv` (585 rows, 94 products) + `output_images_94products.zip` (813MB, all 94 `output/<SKU>/` folders, 580 images) via `SendUserFile`. Flagged to the user: the zip is too large for email (most attachment caps are ~25MB) — needs a cloud link (Drive/Dropbox/WeTransfer) — and the CSV's `Image Src` still holds local paths, not importable until devs host the images and `shopify_export.py --image-base-url https://...` is re-run against that host.
Separately: the 6 SKUs in `backlog-products-image-failed/` (5 from the main batch + Justice League) wait on the user for manual images. `TGS-FIG-FOOTBALL-STAR` (all 3 variants — Al-Nassr, Man United, Real Madrid — every photo is box art, none show the figures out of box) is **not** waiting on anything: user decided 2026-09-23 to keep the existing box photos rather than reshoot.
