.venv/
__pycache__/
*.pyc
# generated outputs (regenerate anytime)
output/
gallery_out/
hero/
gallery_out/**
# large / machine-specific model files
*.onnx
# Draw Things: fetched by setup_drawthings.sh (third-party, not vendored)
generated/
dt_certs/
# scenes you export from Draw Things (per machine)
backgrounds/
woocommerce_import.csv
input/**/product.json
shopify_import.csv
# raw product photos — too large for GitHub (>2 GB); shared via products.zip
raw_footages/
_duplicates_already_processed/
products.zip
input/**/*.jpg
input/**/*.jpeg
# same reasoning as input/ above -- these hold raw shoot photos too (low-stock and
# image-generation-failed products moved out of input/, not a separate photo source)
backlog-products/**/*.jpg
backlog-products/**/*.jpeg
backlog-products/**/product.json
backlog-products-image-failed/**/*.jpg
backlog-products-image-failed/**/*.jpeg
backlog-products-image-failed/**/product.json
backlog-products-discontinued/**/*.jpg
backlog-products-discontinued/**/*.jpeg
backlog-products-discontinued/**/product.json
.DS_Store
photo_labels.json
photo_labels.json.tmp
# everything that is not the catalog (original shoots, superseded photos, pilot runs)
_archive/
# supplier price list (business data, not code)
happy_toys_prices.csv
recovered-photos/
